/*What does the following program do?*/

/*
#include <stdio.h>

// function main begins program execution
 int main( void )
 {
     unsigned int x;
     unsigned int y;
     unsigned int i;
     unsigned int j;
    printf( "%s", "Enter two unsigned integers in the range 1-20: " );
    scanf( "%u%u", &x, &y );
// loop from y to x and prints @ according to the input
    for ( i = 1; i <= y; ++i ) {
        for ( j = 1; j <= x; ++j ) {
            printf( "%s", "@" );
    }
        puts( "" );
    }
 }*/
