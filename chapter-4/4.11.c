/*(Find the Smallest) Write a program that finds the smallest of several integers. Assume that
the first value read specifies the number of values remaining.*/

/*
#include <stdio.h>
#include <limits.h>
int main(){
    int flag,num,min=INT_MAX;
    scanf("%d",&flag);
    for (int i = 0; i <flag; ++i) {
        scanf("%d",&num);
        if(num<min) min=num;
    }
    printf("%d",min);
}*/
