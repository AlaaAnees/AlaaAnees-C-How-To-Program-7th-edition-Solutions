/* Find the error in each of the following. (Note: There may be more than one error.)
 * */


/*
#include <stdio.h>
int main(){
    // x in not declared
    // there should be curly brackets for the for statement.
   // for is capitalized.
   // ; is used to split expressions

    For ( x = 100, x >= 1, ++x )
    printf( "%d\n", x );

    // code modified
    int x;
    for ( x = 100;x >= 1; ++x){
        printf( "%d\n", x );
    }


}*/
