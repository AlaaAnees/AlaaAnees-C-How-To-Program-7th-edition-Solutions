/*
#include <stdio.h>
int main(){
    // y is not declared.
    // if y==1 or y equals anything > 0 , program would run infinitly.
    int y=1;
    while ( y > 0 ) {
        printf( "%d\n", y );
        ++y;
    }
}*/
