/*
#include <stdio.h>
int main(){
    // age is not declared.
    // if statment should'nt have a semicolon after the expression.
    // can't use else statement without having if before it.
    if ( age >= 65 );
    puts( "Age is greater than or equal to 65" );
    else
    puts( "Age is less than 65" );
}*/
