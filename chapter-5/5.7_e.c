/*#include <stdio.h>
void product( void )
{
    int a, b, c, result;
    // missing a semicolon in printf statement
    printf( "%s", "Enter three integers: " )
    scanf( "%d%d%d", &a, &b, &c );
    result = a * b * c;
    printf( "Result is %d", result );
    // no return statement should be returned because it's a void method
    return result;
}
int main(){

}*/
