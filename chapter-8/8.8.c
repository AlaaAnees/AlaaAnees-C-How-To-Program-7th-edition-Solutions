/*(Converting Strings to Floating Point for Calculations) Write a program that inputs four
strings that represent floating-point values, converts the strings to double values, sums the values
and prints the total of the four values.*/

/*
#include <stdio.h>
#include <stdlib.h>
#define SIZE 100
int main(){
    printf("Enter four strings that represent floating-point: ");
    char s1[SIZE],s2[SIZE],s3[SIZE],s4[SIZE];
    gets(s1);
    gets(s2);
    gets(s3);
    gets(s4);
    printf("the sum is:%f", atof(s1)+atof(s2)+atof(s3)+atof(s4));

}
*/
