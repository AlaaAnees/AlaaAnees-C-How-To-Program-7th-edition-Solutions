/*(Printing a Question Mark) Write a program that determines whether ? can be printed as
part of a printf format control string as a literal character rather than using the \? escape sequence.*/

/*
#include <stdio.h>
int main(){

    // Check if ? can be printed as a literal character in printf format control string
    if (printf("%c", '?')) {
        printf("Yes, ? can be printed as a literal character in printf format control string.\n");
    } else {
        printf("No, ? cannot be printed as a literal character in printf format control string.\n");
    }
}
*/
