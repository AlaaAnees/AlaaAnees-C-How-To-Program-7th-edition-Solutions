/*(Outputting a Number with the Floating-Point Conversion Specifiers) Write a program
that uses each of the conversion specifiers e, f and g to input the value 1.2345. Print the values of
each variable to prove that each conversion specifier can be used to input this same value.*/

/*
#include <stdio.h>
int main(){
    float a;

    printf("a: enter 1.2345: ");
    scanf("%e",&a); //exponential value
    printf("%%e  = %e\n",a);


    printf("\n---- b %%f ----\n");

    printf("a: enter 1.2345: ");
    scanf("%f",&a);
    printf("%%f  = %f\n",a);


    printf("\n---- b %%g ----\n");

    printf("a: enter 1.2345: ");
    scanf("%g",&a); //print the decimal floating-point values, and it uses the fixed precision, i.e., the value after the decimal in input would be exactly the same as the value in the output.
    printf("%%g  = %g\n",a);



}
*/
