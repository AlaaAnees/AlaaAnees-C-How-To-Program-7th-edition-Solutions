/*(Reading Strings in Quotes) In some programming languages, strings are entered surrounded
by either single or double quotation marks. Write a program that reads the three strings suzy,
"suzy" and 'suzy'. Are the single and double quotes ignored by C or read as part of the string?*/

/*
#include <stdio.h>
int main(){
    char a[10],b[10],c[10];
    printf("Please type suzy: ");
    gets(a);
    printf("input1 = %s\n", a);


    printf("Please type \'suzy\': ");
    gets(b);
    printf("input2 = %s\n", b);


    printf("Please type \"suzy\": ");
    gets(c);
    printf("input3 = %s\n", c);
}
*/
